import styled from "styled-components";

export const ForContactButton = () => {
    return (
        <>
            <NavContainer>
                <button></button>
            </NavContainer>
        </>
    )
};

const NavContainer = styled.div`

`;